<Window x:Class="ObjLoader.Views.CenterPointWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:l="clr-namespace:ObjLoader.Localization"
        xmlns:vm="clr-namespace:ObjLoader.ViewModels"
        mc:Ignorable="d"
        Title="{x:Static l:Texts.Window_CenterPoint}" Height="500" Width="800"
        WindowStartupLocation="CenterOwner" Topmost="True" ResizeMode="CanResize"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">

    <Window.Resources>
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{x:Static l:Texts.Msg_CenterPointDesc}" TextWrapping="Wrap" Margin="10" FontSize="12" Opacity="0.8"/>

                <StackPanel Grid.Row="1" Margin="10,0,10,15">
                    <TextBlock Text="{x:Static l:Texts.Group_Selection}" Opacity="0.7" Margin="0,0,0,5"/>
                    <UniformGrid Columns="3" Rows="1" HorizontalAlignment="Stretch">
                        <Button Command="{Binding SetModeCommand}" CommandParameter="{x:Static vm:SelectionMode.Vertex}" ToolTip="{x:Static l:Texts.Mode_Vertex}" Margin="0,0,-1,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Padding" Value="0,10"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Mode}" Value="Vertex">
                                            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="Panel.ZIndex" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Path Data="M 6,4 A 2,2 0 0,1 8,6 A 2,2 0 0,1 6,8 A 2,2 0 0,1 4,6 A 2,2 0 0,1 6,4 Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform" Width="12" Height="12"/>
                        </Button>

                        <Button Command="{Binding SetModeCommand}" CommandParameter="{x:Static vm:SelectionMode.Edge}" ToolTip="{x:Static l:Texts.Mode_Edge}" Margin="-1,0,-1,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Padding" Value="0,10"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Mode}" Value="Edge">
                                            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="Panel.ZIndex" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Path Data="M2,10 L10,2" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2" Stretch="Uniform" Width="14" Height="14"/>
                        </Button>

                        <Button Command="{Binding SetModeCommand}" CommandParameter="{x:Static vm:SelectionMode.Face}" ToolTip="{x:Static l:Texts.Mode_Face}" Margin="-1,0,0,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Padding" Value="0,10"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Mode}" Value="Face">
                                            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="Panel.ZIndex" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Path Data="M2,2 L10,2 L10,10 L2,10 Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.8" Stretch="Uniform" Width="14" Height="14"/>
                        </Button>
                    </UniformGrid>
                </StackPanel>

                <Grid Grid.Row="2" Margin="10,0,10,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="X" VerticalAlignment="Center" Opacity="0.7" Margin="0,0,10,10"/>
                    <TextBox Grid.Column="1" Text="{Binding CenterX, StringFormat=N3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Text="Y" VerticalAlignment="Center" Opacity="0.7" Margin="0,0,10,10"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CenterY, StringFormat=N3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2" Text="Z" VerticalAlignment="Center" Opacity="0.7" Margin="0,0,10,0"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CenterZ, StringFormat=N3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                </Grid>

                <StackPanel Grid.Row="3" VerticalAlignment="Bottom" Margin="10">
                    <TextBlock Text="{Binding LockStatusText}" HorizontalAlignment="Center" Margin="0,0,0,5" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    <TextBlock Text="{Binding HoveredElementInfo}" Opacity="0.9" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,10" HorizontalAlignment="Center" TextAlignment="Center"/>
                </StackPanel>

                <UniformGrid Grid.Row="4" Columns="1" Rows="2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="{x:Static l:Texts.Button_ApplySelection}" Command="{Binding ApplySelectionCommand}" Padding="0,10" Margin="0" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                        <Button Grid.Column="1" Command="{Binding ResetSelectionCommand}" Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" BorderThickness="1,1,0,0" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" Background="Transparent">
                            <Path Data="M17.65,6.35C16.2,4.9 14.21,4 12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20C15.73,20 18.84,17.45 19.73,14H17.65C16.83,16.33 14.61,18 12,18A6,6 0 0,1 6,12A6,6 0 0,1 12,6C13.66,6 15.14,6.69 16.22,7.78L13,11H20V4L17.65,6.35Z" 
                                  Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                                  Stretch="Uniform" Margin="10"/>
                        </Button>
                    </Grid>
                    <Button x:Name="CloseButton" Content="{x:Static l:Texts.Dialog_Close}" IsCancel="True" Padding="0,10" Margin="0" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                </UniformGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
            <Grid>
                <Viewport3D x:Name="MainViewport" ClipToBounds="True">
                    <Viewport3D.Camera>
                        <PerspectiveCamera Position="{Binding CameraPosition}" 
                                           LookDirection="{Binding CameraLookDirection}" 
                                           UpDirection="0,1,0" 
                                           FieldOfView="45"/>
                    </Viewport3D.Camera>
                    <ModelVisual3D>
                        <ModelVisual3D.Content>
                            <Model3DGroup>
                                <DirectionalLight Color="White" Direction="-1,-1,-1"/>
                                <DirectionalLight Color="#808080" Direction="1,1,1"/>
                                <AmbientLight Color="#404040"/>

                                <GeometryModel3D Geometry="{Binding ModelMesh}">
                                    <GeometryModel3D.Material>
                                        <DiffuseMaterial Brush="Gray"/>
                                    </GeometryModel3D.Material>
                                    <GeometryModel3D.BackMaterial>
                                        <DiffuseMaterial Brush="Gray"/>
                                    </GeometryModel3D.BackMaterial>
                                </GeometryModel3D>

                                <GeometryModel3D Geometry="{Binding HighlightGeometry}">
                                    <GeometryModel3D.Material>
                                        <EmissiveMaterial Brush="#FF0000"/>
                                    </GeometryModel3D.Material>
                                </GeometryModel3D>

                                <GeometryModel3D Geometry="{Binding CenterPointGeometry}">
                                    <GeometryModel3D.Material>
                                        <EmissiveMaterial Brush="#00FF00"/>
                                    </GeometryModel3D.Material>
                                </GeometryModel3D>
                            </Model3DGroup>
                        </ModelVisual3D.Content>
                    </ModelVisual3D>
                </Viewport3D>

                <TextBlock Text="{x:Static l:Texts.Msg_HoverInfo}" 
                           Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" Opacity="0.5" 
                           VerticalAlignment="Top" HorizontalAlignment="Left" 
                           Margin="10" IsHitTestVisible="False"/>
            </Grid>
        </Border>
    </Grid>
</Window>