<Window x:Class="ObjLoader.Views.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:settings="clr-namespace:ObjLoader.ViewModels.Settings"
        xmlns:infra="clr-namespace:ObjLoader.Infrastructure"
        xmlns:util="clr-namespace:ObjLoader.Utilities"
        xmlns:l="clr-namespace:ObjLoader.Localization"
        xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
        mc:Ignorable="d"
        Title="{x:Static l:Texts.SettingsTitle}" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <DataTemplate x:Key="TextTemplate" DataType="{x:Type infra:TextSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Padding="5" IsEnabled="{Binding IsEnabled}"/>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="BoolTemplate" DataType="{x:Type infra:BoolSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2"/>
                <c:ToggleSlider Value="{Binding Value}" HorizontalAlignment="Left" IsEnabled="{Binding IsEnabled}"/>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="RangeTemplate" DataType="{x:Type infra:RangeSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2"/>
                <Grid IsEnabled="{Binding IsEnabled}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Slider Grid.Column="0" Value="{Binding Value}" Minimum="{Binding Min}" Maximum="{Binding Max}" TickFrequency="{Binding Tick}" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <TextBlock Grid.Column="2" Text="{Binding Unit}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </Grid>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="IntSpinnerTemplate" DataType="{x:Type infra:IntSpinnerSettingViewModel}">
            <Grid IsEnabled="{Binding IsEnabled}" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Value}" VerticalAlignment="Center" Margin="0,0,2,0" FontWeight="Bold"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <Button Command="{Binding UpCommand}" Content="▲" FontSize="6" Height="8" Padding="0" BorderThickness="0" Background="Transparent"/>
                    <Button Command="{Binding DownCommand}" Content="▼" FontSize="6" Height="8" Padding="0" BorderThickness="0" Background="Transparent"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="EnumTemplate" DataType="{x:Type infra:EnumSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2"/>
                <ComboBox ItemsSource="{Binding EnumValues}" 
                          SelectedValue="{Binding Value}" 
                          SelectedValuePath="Value" 
                          Padding="5" 
                          IsEnabled="{Binding IsEnabled}"
                          HorizontalContentAlignment="Left"
                          VerticalContentAlignment="Center">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="true">
                                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="false">
                                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Label}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="ColorTemplate" DataType="{x:Type infra:ColorSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2"/>
                <ToolBar Background="Transparent" BorderThickness="0" Padding="0" Margin="0,0,8,0" HorizontalAlignment="Stretch">
                    <ToolBar.Template>
                        <ControlTemplate TargetType="ToolBar">
                            <ItemsPresenter/>
                        </ControlTemplate>
                    </ToolBar.Template>
                    <ToolBar.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Grid/>
                        </ItemsPanelTemplate>
                    </ToolBar.ItemsPanel>
                    <c:ColorPicker Value="{Binding ColorValue, Mode=TwoWay}" Height="26" HorizontalAlignment="Stretch" IsEnabled="{Binding IsEnabled}"/>
                </ToolBar>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="FileTemplate" DataType="{x:Type infra:FilePathSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2"/>
                <Grid IsEnabled="{Binding IsEnabled}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Padding="5" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="..." Command="{Binding SelectCommand}" Width="30" Margin="5,0,0,0" Padding="5"/>
                </Grid>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="ButtonTemplate" DataType="{x:Type infra:ButtonSettingViewModel}">
            <Button Content="{Binding Label}" Command="{Binding Command}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,5" Padding="10,5" HorizontalAlignment="Left" IsEnabled="{Binding IsEnabled}"/>
        </DataTemplate>

        <util:SettingTemplateSelector x:Key="SettingSelector"
                                      TextTemplate="{StaticResource TextTemplate}"
                                      BoolTemplate="{StaticResource BoolTemplate}"
                                      RangeTemplate="{StaticResource RangeTemplate}"
                                      EnumTemplate="{StaticResource EnumTemplate}"
                                      ColorTemplate="{StaticResource ColorTemplate}"
                                      FileTemplate="{StaticResource FileTemplate}"
                                      ButtonTemplate="{StaticResource ButtonTemplate}"
                                      IntSpinnerTemplate="{StaticResource IntSpinnerTemplate}"/>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,0,1,0">
            <TreeView ItemsSource="{Binding Groups}" Background="Transparent" BorderThickness="0">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" MinWidth="19"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <ToggleButton x:Name="Expander" ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                            <ToggleButton.Style>
                                                <Style TargetType="{x:Type ToggleButton}">
                                                    <Setter Property="Focusable" Value="False"/>
                                                    <Setter Property="Width" Value="16"/>
                                                    <Setter Property="Height" Value="16"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                                <Border x:Name="ExpanderBorder" Background="Transparent" Height="16" Padding="5" Width="16">
                                                                    <Path x:Name="ExpandPath" Data="M0,0 L0,6 L6,0 z" 
                                                                          Fill="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" 
                                                                          Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
                                                                        <Path.RenderTransform>
                                                                            <RotateTransform Angle="135" CenterX="3" CenterY="3"/>
                                                                        </Path.RenderTransform>
                                                                    </Path>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsChecked" Value="True">
                                                                        <Setter Property="RenderTransform" TargetName="ExpandPath">
                                                                            <Setter.Value>
                                                                                <RotateTransform Angle="180" CenterX="3" CenterY="3"/>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                    </Trigger>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Fill" TargetName="ExpandPath" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                                                        <Setter Property="Stroke" TargetName="ExpandPath" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ToggleButton.Style>
                                        </ToggleButton>

                                        <Border x:Name="Bd" Grid.Column="1" BorderThickness="0" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                            <ContentPresenter x:Name="PART_Header" ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Border>
                                        <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Grid.Row="1"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsExpanded" Value="false">
                                            <Setter Property="Visibility" TargetName="ItemsHost" Value="Collapsed"/>
                                        </Trigger>
                                        <Trigger Property="HasItems" Value="false">
                                            <Setter Property="Visibility" TargetName="Expander" Value="Hidden"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Path Grid.Column="0" Data="{Binding IconData}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,10,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="14" VerticalAlignment="Center"/>
                            <ItemsControl Grid.Column="2" ItemsSource="{Binding HeaderItems}" ItemTemplateSelector="{StaticResource SettingSelector}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Margin" Value="5,0,0,0"/>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                            </ItemsControl>
                        </Grid>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="20">
                <ItemsControl ItemsSource="{Binding SelectedGroup.Items}" ItemTemplateSelector="{StaticResource SettingSelector}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Margin" Value="0,0,0,15"/>
                            <Setter Property="infra:MouseObserver.Observe" Value="True"/>
                            <Setter Property="infra:MouseObserver.IsHovered" Value="{Binding IsHovered, Mode=TwoWay}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsDependent}" Value="True">
                                    <Setter Property="Opacity" Value="1.0"/>
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsDependent}" Value="True"/>
                                        <Condition Binding="{Binding IsEnabled}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Opacity" Value="0.5"/>
                                </MultiDataTrigger>
                                <DataTrigger Binding="{Binding IsDependent}" Value="True">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <Border BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="2,0,0,0" Padding="10,0,0,0">
                                                    <ContentPresenter Content="{Binding}" ContentTemplateSelector="{StaticResource SettingSelector}"/>
                                                </Border>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </ScrollViewer>

            <Border Grid.Row="1" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,1,0,0" Padding="15,10" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Opacity="0.8"/>
            </Border>

            <Border Grid.Row="2" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Padding="10" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ItemsControl Grid.Column="0" ItemsSource="{Binding LeftButtons}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Label}" Command="{Binding Command}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,0,10,0" Padding="15,5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl Grid.Column="2" ItemsSource="{Binding RightButtons}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Label}" Command="{Binding Command}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="10,0,0,0" Padding="15,5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>