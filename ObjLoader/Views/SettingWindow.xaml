<Window x:Class="ObjLoader.Views.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ObjLoader.ViewModels"
        xmlns:infra="clr-namespace:ObjLoader.Infrastructure"
        xmlns:util="clr-namespace:ObjLoader.Utilities"
        xmlns:l="clr-namespace:ObjLoader.Localization"
        xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
        mc:Ignorable="d"
        Title="{x:Static l:Texts.SettingsTitle}" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <DataTemplate x:Key="TextTemplate" DataType="{x:Type infra:TextSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="BoolTemplate" DataType="{x:Type infra:BoolSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                <c:ToggleSlider Value="{Binding Value}" HorizontalAlignment="Left"/>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="RangeTemplate" DataType="{x:Type infra:RangeSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Value="{Binding Value}" Minimum="{Binding Min}" Maximum="{Binding Max}" TickFrequency="{Binding Tick}" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <TextBlock Grid.Column="2" Text="{Binding Unit}" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                </Grid>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="EnumTemplate" DataType="{x:Type infra:EnumSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                <ComboBox ItemsSource="{Binding EnumValues}" SelectedValue="{Binding Value}" DisplayMemberPath="Label" SelectedValuePath="Value" Padding="5"/>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="ColorTemplate" DataType="{x:Type infra:ColorSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                <c:ColorPicker Value="{Binding ColorValue, Mode=TwoWay}" Height="26" HorizontalAlignment="Stretch" Margin="0,0,8,0"/>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="FileTemplate" DataType="{x:Type infra:FilePathSettingViewModel}">
            <DockPanel Margin="0,5" Background="Transparent">
                <TextBlock Text="{Binding Label}" DockPanel.Dock="Top" Margin="0,0,0,2" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Padding="5" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="..." Command="{Binding SelectCommand}" Width="30" Margin="5,0,0,0" Padding="5"/>
                </Grid>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="ButtonTemplate" DataType="{x:Type infra:ButtonSettingViewModel}">
            <Button Content="{Binding Label}" Command="{Binding Command}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,5" Padding="10,5" HorizontalAlignment="Left"/>
        </DataTemplate>

        <util:SettingTemplateSelector x:Key="SettingSelector"
                                      TextTemplate="{StaticResource TextTemplate}"
                                      BoolTemplate="{StaticResource BoolTemplate}"
                                      RangeTemplate="{StaticResource RangeTemplate}"
                                      EnumTemplate="{StaticResource EnumTemplate}"
                                      ColorTemplate="{StaticResource ColorTemplate}"
                                      FileTemplate="{StaticResource FileTemplate}"
                                      ButtonTemplate="{StaticResource ButtonTemplate}"/>

        <Style TargetType="ListBoxItem" x:Key="GroupListItemStyle">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Opacity" Value="0.8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="Bd" BorderThickness="0,0,0,0" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Opacity" Value="1.0"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="4,0,0,0"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Opacity" Value="1.0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,0,1,0">
            <ListBox ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" 
                     ItemContainerStyle="{StaticResource GroupListItemStyle}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{Binding IconData}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Title}" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="20">
                <ItemsControl ItemsSource="{Binding SelectedGroup.Items}" ItemTemplateSelector="{StaticResource SettingSelector}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Margin" Value="0,0,0,15"/>
                            <Setter Property="infra:MouseObserver.Observe" Value="True"/>
                            <Setter Property="infra:MouseObserver.IsHovered" Value="{Binding IsHovered, Mode=TwoWay}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </ScrollViewer>

            <Border Grid.Row="1" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,1,0,0" Padding="15,10" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" Opacity="0.8"/>
            </Border>

            <Border Grid.Row="2" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Padding="10" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ItemsControl Grid.Column="0" ItemsSource="{Binding LeftButtons}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Label}" Command="{Binding Command}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,0,10,0" Padding="15,5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl Grid.Column="2" ItemsSource="{Binding RightButtons}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Label}" Command="{Binding Command}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="10,0,0,0" Padding="15,5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>