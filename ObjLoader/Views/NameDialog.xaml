<Window x:Class="ObjLoader.Views.NameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:ObjLoader.Localization"
        Title="{x:Static l:Texts.Dialog_Name}" Height="150" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Static l:Texts.Dialog_Name}" Margin="0,0,0,5" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="{x:Static l:Texts.Dialog_Ok}" Click="Ok_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="{x:Static l:Texts.Dialog_Cancel}" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>